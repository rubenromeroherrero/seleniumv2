package v3_servidoresYanalisisDOM;

public class testv8_staticServer {

    public static void main(String[] args) {
        // 1-Instalamos a nivel global "npm i -g static-server"
        // 2-Indicamos la ruta en la que se encuentra la carpeta con nuestro index
        // 3-Una vez tenemos la ruta en la consola "static-server"
        // 4-Levantado el servidor estático http://localhost:9080
        // De esta manera lo probamos con el protocolo https [servidor estático]
    }
}
